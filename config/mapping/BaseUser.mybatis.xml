<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.littleprincess.system.UserEntity">
    <resultMap type="cn.littleprincess.system.po.BaseUser" id="BaseUserMap">
        <result property="id" column="id"></result>
        <result property="username" column="user_name"></result>
        <result property="password" column="password"></result>
        <result property="sex" column="sex"></result>
        <result property="birthday" column="birthday"></result>
        <result property="lastIp" column="last_ip"></result>
        <result property="lastVisit" column="last_visit"></result>
        <result property="credits" column="credits"></result>
        <result property="createTime" column="create_time"></result>
        <result property="updateTime" column="update_time"></result>
    </resultMap>

    <select id="hasMatchUser" resultMap="BaseUserMap" parameterType="java.util.HashMap">
        select *
        from t_user
        where 1=1
        <if test="username!=null">
            and user_name = #{username}
        </if>
        <if test="password!=null">
            and password = #{password}
        </if>
        order by id desc
    </select>

    <select id="findUserByName" resultMap="BaseUserMap" parameterType="String">
        select *
        from t_user
        where 1=1
        and user_name = #{username}
    </select>

    <update id="update">
        <![CDATA[
            update  t_user 
            set     user_name=#{username, jdbcType=VARCHAR},
                    sex=#{sex, jdbcType=NUMERIC},
                    birthday=#{birthday, jdbcType=DATE},
                    last_ip=#{lastIp, jdbcType=VARCHAR},
                    credits=#{credits, jdbcType=NUMERIC} 
            WHERE   id = #{id, jdbcType=NUMERIC}
        ]]>
    </update>

    <select id="findAll" resultMap="BaseUserMap">
        select * from t_user
    </select>
    
    <select id="pageUsers" resultMap="BaseUserMap">
        select * from t_user
    </select>
    <insert id="add" parameterType="cn.littleprincess.system.po.BaseUser">
        <![CDATA[
            INSERT INTO t_user (
                user_name,
                sex,
                birthday
            ) VALUES (
                #{username, jdbcType=VARCHAR},
                #{sex, jdbcType=NUMERIC},
                #{birthday}
            )
        ]]>
        <selectKey resultType="long" keyProperty="id">
            SELECT LAST_INSERT_ID() AS ID
        </selectKey>
    </insert>
    
    <delete id="delete">
        <![CDATA[
            DELETE from t_user 
            WHERE id = #{id}
        ]]> 
    </delete>
    
    <select id="findById" resultMap="BaseUserMap" parameterType="java.lang.Long">
        select * from t_user
        where id = #{id}
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.mypandora.system.LogEntity">
    <resultMap type="cn.mypandora.system.po.BaseLog" id="BaseLogMap">
        <result property="id" column="id"></result>
        <result property="name" column="name"></result>
        <result property="description" column="description"></result>
        <result property="ip" column="ip"></result>
        <result property="createTime" column="create_time"></result>
        <result property="updateTime" column="update_time"></result>
    </resultMap>

    <select id="findAll" resultMap="BaseLogMap">
        select * from t_base_log
    </select>
    
    <select id="pageLogs" resultMap="BaseLogMap" parameterType="java.util.HashMap">
        SELECT *
        FROM t_base_log
        WHERE 1=1
        <if test="id!=null">
            AND id = #{id}
        </if>
        ORDER BY create_time DESC
    </select>
    
    <insert id="add" parameterType="cn.mypandora.system.po.BaseLog">
        <![CDATA[
            INSERT INTO t_base_log (
                name,
                description,
                ip
            ) VALUES (
                #{name, jdbcType=VARCHAR},
                #{description, jdbcType=VARCHAR},
                #{ip, jdbcType=VARCHAR}
            )
        ]]>
        <selectKey resultType="long" keyProperty="id">
            SELECT LAST_INSERT_ID() AS ID
        </selectKey>
    </insert>
    
    <delete id="delete">
        <![CDATA[
            DELETE from t_base_log 
            WHERE id = #{id}
        ]]> 
    </delete>
</mapper>